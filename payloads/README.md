# payloads

Tiny rv64 acceptance-test ELFs and their build script live here.

Empty until M3 (first U-mode ELF payload running to `sys_exit`). Kept in the
tree now so the layout matches CLAUDE.md from day one.
