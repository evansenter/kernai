[workspace]
resolver = "2"
members = [
    "sys",
    "hello",
    "crasher",
    "muzzled",
    "spawner",
    "child",
    "runaway",
    "wild",
    "wxviol",
]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
debug = true
